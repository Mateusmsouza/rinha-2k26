events {}
http {
    upstream backend {
        server app_0:5000;
        server app_1:5000;
    }
    server {
        listen 9999;
        location / {
            proxy_pass http://backend;
        }
    }
}
