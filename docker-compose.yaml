services:
  app_0:
    platform: linux/amd64
    build: .
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 165MB

  app_1:
    platform: linux/amd64
    build: .
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 165MB

  lb:
    platform: linux/amd64
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "9999:9999"
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 20MB
    depends_on:
      - app_0
      - app_1
